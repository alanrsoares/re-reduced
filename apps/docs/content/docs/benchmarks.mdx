---
title: Benchmarks
description: How re-reduced compares to Zustand and useReducer on renders and throughput.
---

Every number below is rendered from
[`bench-results.json`](https://github.com/alanrsoares/re-reduced/blob/main/apps/docs/lib/bench-results.json),
which [`bench/src/capture.ts`](https://github.com/alanrsoares/re-reduced/blob/main/bench/src/capture.ts)
writes from a real [mitata](https://github.com/evanwashere/mitata) `measure()`
run — nothing here is hand-typed, so the page can't drift from the harness. The
render-count claim is asserted by a committed test. Reproduce it all yourself:

- `bun run --filter @re-reduced/bench capture` — regenerate the data on _your_ machine
- `bun run --filter @re-reduced/bench bench:all` — the same probes, printed
- `bun test bench` — the render-count comparison

<BenchProvenance />

<Callout type="info">
  Microbenchmarks are **indicative**, not a leaderboard — your hardware, your
  workload, and React's own render cost all move the needle. The point is the
  _shape_ of each curve, not the third significant figure.
</Callout>

## Fine-grained re-renders (the headline)

A container with `{ a, b }`; one leaf reads `a`, another reads `b`. Bump `a` and
count which leaves re-render (asserted in
[`render-count.spec.tsx`](https://github.com/alanrsoares/re-reduced/blob/main/bench/test/render-count.spec.tsx)):

| Approach | `a` leaf | `b` leaf |
| --- | --- | --- |
| **re-reduced** (`useSelect`) | re-renders | **bails out** |
| Zustand (selector) | re-renders | **bails out** |
| `useReducer` + Context | re-renders | re-renders |

re-reduced matches Zustand's per-component bail-out, and both avoid the
broadcast re-render a naive `useReducer` + Context tree suffers. With re-reduced
the granularity is **structural** (per-field signals) — you don't hand-write
equality selectors to get it.

## Dispatch throughput

A `count + 1` transition, dispatched + read in a tight loop (no React):

<BenchThroughput />

re-reduced is in the same class as Zustand — the cost is a signal write plus a
shallow diff, which is what buys the fine-grained updates above. At UI event
rates it's far below the noise floor.

### How dispatch scales with state size

A handler returns only the keys it changes; the store writes exactly those
signals and keeps a plain-object mirror in lockstep, so it never peeks the whole
container per dispatch ([ADR-0010](https://github.com/alanrsoares/re-reduced/blob/main/docs/adr/0010-action-handlers-merge-returned-keys.md)).
Bumping **one** field in an `N`-field container:

<BenchScaling />

re-reduced dispatch is **O(changed keys)** — flat regardless of container size.
Zustand grows with field count here because each `set` allocates a fresh merged
state object. The cost is to write a one-field handler as `(s) => ({ count:
s.count + 1 })` — no spread; the returned keys are merged.

> Earlier this curve ran the other way: handlers were typed to return the full
> state, forcing a `...s` spread and an O(fields) snapshot + diff every dispatch.
> The mirror plus partial returns removed both — without a lazy proxy (that was
> tried and abandoned at ~7× slower, since spreads enumerate every field through
> the proxy traps).

## Derivation reads

An expensive derivation read repeatedly while its input is unchanged — the
scenario memoization targets:

<BenchDerive />

A memoized `derive` read is a flat dependency-version check regardless of how
heavy the computation is — but that floor isn't zero. Below the crossover (a
handful of items, e.g. `count * 2`), recomputing is cheaper than the cached
read. Rule of thumb: reach for `derive` when the computation is non-trivial or
read far more often than its inputs change; skip it for trivial scalar math.

## Takeaways

- **Renders:** fine-grained by construction, on par with Zustand, ahead of
  Context-based reducers.
- **Dispatch:** **O(changed keys)** — flat regardless of container size, because
  handlers return only what changed and the store writes just those signals.
  Zustand-class (or ahead) at small state; pulls ahead as the container grows.
- **Derivations:** memoized — a large win above the crossover, a small loss
  below it. Use `derive` for non-trivial computed values read repeatedly.
