{
  "name": "@re-reduced/example-preact-todo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "bun test"
  },
  "dependencies": {
    "@preact/signals": "^1.3.0",
    "@re-reduced/adapter-kit": "workspace:*",
    "@re-reduced/demos": "workspace:*",
    "@re-reduced/preact": "workspace:*",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "@testing-library/preact": "^3.2.4",
    "vite": "^6.0.0"
  }
}
